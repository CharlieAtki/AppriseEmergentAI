name: CI

on:
  pull_request:
    branches: [main]
    paths:
      - "backend/**"
      - ".pre-commit-config.yaml"
      - "backend/ruff.toml"
  push:
    branches: [main]
    paths:
      - "backend/**"

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: Install workspace deps
        run: uv sync --all-packages

      - name: Ruff lint
        run: uvx ruff check backend/

      - name: Ruff format check
        run: uvx ruff format --check backend/

      - name: mypy (core)
        run: cd backend && uv run --with mypy mypy core/core/ --ignore-missing-imports

      - name: mypy (api)
        run: cd backend && uv run --with mypy mypy api/api/ --ignore-missing-imports

      - name: mypy (worker)
        run: cd backend && uv run --with mypy mypy worker/worker/ --ignore-missing-imports

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Bandit SAST
        run: uvx bandit -r backend/ -ll --exclude backend/core/core/migrations,backend/worker/tests,backend/core/tests

      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@0.30.0
        with:
          scan-type: fs
          scan-ref: backend/
          severity: HIGH,CRITICAL
          exit-code: "1"
          format: table

  test:
    name: Unit & Integration Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install worker test deps
        run: cd backend && uv sync --package apprise-worker --extra test

      - name: Run worker tests
        run: cd backend && uv run --package apprise-worker pytest worker/tests/ -v

      - name: Install core test deps
        run: cd backend && uv sync --package apprise-core --extra test

      - name: Run core tests
        run: cd backend && uv run --package apprise-core pytest core/tests/ -v
