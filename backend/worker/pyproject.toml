[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "apprise-worker"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "apprise-core",
]

[tool.hatch.build.targets.wheel]
packages = ["worker"]
