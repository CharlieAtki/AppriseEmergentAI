[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "apprise-worker"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "apprise-core",
    "arq>=0.26",
    "httpx>=0.27",
]

[tool.hatch.build.targets.wheel]
packages = ["worker"]
