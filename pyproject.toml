[project]
name = "apprise"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[tool.uv.workspace]
members = ["backend/core", "backend/api", "backend/worker"]

[tool.uv.sources]
apprise-core = { workspace = true }
